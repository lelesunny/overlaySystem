import { Component, Input, Output, EventEmitter, SimpleChange, ElementRef, OnInit } from '@angular/core';
import { Router } from '@angular/router'

@Component({
    selector: 'dotted-border',
    templateUrl: './dotted-border.component.html',
    styleUrls: ['./dotted-border.component.css']
})
export class DottedBorderComponent {
}
